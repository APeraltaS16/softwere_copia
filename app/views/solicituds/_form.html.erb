<%= form_with(model: solicitud, local: true) do |form| %>
  <% if solicitud.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(solicitud.errors.count, "error") %> prohibited this solicitud from being saved:</h2>

      <ul>
        <% solicitud.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

  <div class="field">
    <%= form.label :dia_reserva %>
    <%= form.date_select :dia_reserva %>
  </div>

  <div class="field">
    <%= form.label :hora_reserva %>
    <%= form.time_select :hora_reserva %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
